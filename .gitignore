# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files — never commit real secrets
.env
.env*.local
# Step 54 — isolated test-database credentials (same shape as .env; see
# scripts/README.md). Never committed, exactly like .env itself.
.env.test

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts
